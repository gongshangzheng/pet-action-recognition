# 周报 — 郑鑫裕 — 2026 第 W33 周（08-10 ~ 08-16）

## 一、本周一句话

**主线四条齐头并进**：① **t12 动作识别精度攻坚** — 3 个 cats-v1 模型微调 + speedrun 评测（tsm-resnet50 92% 当前最优，drinking 0/6 全误验证了样本不足判断）；② **t13-1 YOLO11 检测准备** — zero-shot 体检 + 9 类标注定稿（夜视漏检 89%，微调必须做）；③ **Live 模块本周基本完成**（8/9 子任务 closed，剩 t11-6 路由+导航）；④ **Speed Run 三项 OpenSpec 变更上线**（run_name 批次、--custom 微调模型、--ann-file GT 来源）。同时按研究计划重排里程碑 + 新增 t12-t15 任务树作为二期执行依据；识别出 4 项任务书与现有计划的缺口，需要负责人协调。

## 二、对决策有影响的关键发现

### 体检数据：预训练目标检测模型直接测试不可用，微调必须做

体检在 GPU 服务器（4090）上用公开预训练的目标检测模型对全部 79 段 cats 视频跑了一遍（未做任何训练直接测试），关键数字：

| 场景 | 猫的检出率 | 含义 |
|---|---|---|
| **夜间红外帧**（家用摄像头夜间自动切换的成像模式） | **10-11%** | 公开训练集里没有红外样本，漏检率约 89% |
| 白天帧（按画面亮度判断） | 35% | 数字看似不低，但分母含大量"没猫"的空帧 |
| **有猫帧的召回代理**（人工标注的猫行为段内） | **48.5-48.7%** | 修正空帧稀释后的真实水平 |
| 形态：猫在盆边（进食/饮水） | **91-99%** | 标准姿态，公开模型擅长 |
| 形态：猫蜷缩/趴卧（舔毛/静止） | **10-24%** | 非典型姿态，公开模型基本失效 |

**结论**：YOLO11 检测架构可用，端侧档位（轻量版）即可满足精度要求，但必须用我们的数据微调；夜视帧 + 蜷缩形态两个盲区决定 9 月标注必须重点覆盖这两类帧。

### 微调根因：9 类标注中 COCO 直接覆盖的只有 2 类

| 类目 | 来源 | 是否需要人工标 |
|---|---|---|
| `cat`（猫） | COCO 原生 | 不需要 |
| `person`（人） | COCO 原生 | 不需要 |
| `food_bowl` / `water_bowl`（食盆/水盆） | 项目自定义（COCO 只有笼统的 "bowl"） | **需要**——且需人工区分食/水 |
| `litter_box`（猫砂盆） | 项目自定义 | **需要** |
| `toy`（猫玩具） | 项目自定义 | **需要** |
| `door_window`（门窗） | 项目自定义 | **需要** |
| `cat_face`（猫脸） | 本周新增，为支撑中期验收"面部暴露占比"指标 | **需要** |
| `bowl_unspecified`（盆待细分） | 机器占位类（模型无法区分时占位） | 人工必须修正为 food_bowl/water_bowl |

预训练模型可直接覆盖 2 个类（cat、person），其余 6+ 个类全靠 9 月人工标注——这是"微调必须做"的根本原因，也是为什么"标注配比"（夜视帧 + 蜷缩形态重点覆盖）必须现在定下来。

### 本周训练与 speedrun 评测（t12 主线）

| 模型 | top1 acc (speedrun) | eating | drinking | grooming | activity | stationary |
|---|---|---|---|---|---|---|
| **tsm-resnet50** | **92%**（93/101） | 98% | **0%** | 100% | 94% | 100% |
| slowonly-resnet50 | 91%（92/101） | 100% | **0%** | 100% | 82% | 100% |
| timesformer-divst | 81%（82/101） | 100% | **0%** | 40% | 71% | 87% |

**关键发现**：

- **tsm-resnet50 当前最优**：speedrun 上 92% 比 slowonly-resnet50（91%）略高、比 timesformer-divst（81%）显著好
- **drinking 6/6 全误**（三个模型一致）——直接验证了"drinking 类仅 9 段来自 2 段原始视频、样本不足以学习"的判断，是 t12-1 数据审计里 drinking 决策的**实测铁证**
- **timesformer-divst 的 grooming 仅 40%**（其他模型 100%）——与 t10 骨架路线"舔毛蜷缩形态域外"判断一致
- **speedrun 是少量测试切片**（每类 ~6-17 段），92% 不代表 cats_v1 整体精度，但说明**微调方向对、tsm-resnet50 是当前最优候选**

> 含义：t12-1 数据审计的"drinking 并入 eating"决策获得了硬证据，9 月正式评测时无须再讨论。

### Live 模块本周基本完成（t11）

8/9 子任务本周 closed：

| 子任务 | 状态 | 内容 |
|---|---|---|
| t11-1 后端 live router | ✓ | 源 CRUD + 流代理 + 截屏上传 |
| t11-2 前端 Live 骨架 | ✓ | 源列表 + 视频流播放 |
| t11-3/4/5 PTZ + 截屏 + 源管理 UI | ✓ | 虚拟云台摇杆、canvas 截屏、源增删改查表单 |
| t11-7 SSE 实时推理 | ✓ | 后端 SSE 端点（decord 滑窗 + 视频模型 forward + VLM 切片） |
| t11-8/9 前端实时叠加 + 模型选择 | ✓ | EventSource 接 SSE + 时间轴色块 OSD + 27 模型 registry 选模型 |
| t11-6 路由 + 导航 + wiki 链接 | ⏳ planned | 唯一未完成项 |

**关键修复**：`fix(live)` 解决 CUDA init 在 decord 之后导致 `std::random_device` 读 /dev/urandom 失败的 bug——GPU 推理可用（之前只能 CPU）。这对线上推理 demo 是关键路径。

### Speed Run 三个 OpenSpec 变更上线

| 变更 | 内容 | 影响 |
|---|---|---|
| `run_name 批次` | API 接受/生成/过滤 run_name，前端按批次筛选 | 度小满评估多批微调模型时的组织工具 |
| `--custom 微调模型` | speedrun 支持加载微调后的 checkpoint（不是只能跑预训练） | 把本周 t12 训练的 tsm-resnet50 checkpoint 直接接到 speedrun 验证端到端 |
| `--ann-file GT 来源` | 速度运行可指定自定义标注文件作 GT | 替换内置标注，支持 cats 等新数据集的 speedrun |

三项变更全部完成并归档（speedrun 权威入口路径已统一到 `tools/mtdocx-skills/speedrun`）。

### 二期路线按研究计划重排（关键管理动作）

按任务书阶段表（7-8月 / 9-11月 / 11月底中期 / 12-1月 / 1-2月 / 2月结项）重新排列里程碑 + 新增任务树 t12/t13/t14/t15 作为二期执行依据：

- **P0 精度攻坚**（t12，8月中-9月底）：数据审计修复、两阶段微调、漏检率口径
- **P1 端侧 pipeline**（t13，9-11月）：YOLO11 检测、BoT-SORT 跟踪、事件引擎
- **P2 部署链路**（t14，11-12月）：ONNX 导出 → RKNN 转换（依赖硬件方工具链）
- **P2 中期集成**（t15，10月起）：上报统计页、回放、亮点抓取

### 任务书与现有计划的 4 项缺口

| 缺口 | 任务书要求 | 现状 | 影响 |
|---|---|---|---|
| 音频事件识别 | 功能 1 明确列出 YAMNet（猫叫、撞击声） | phase2 计划标"暂缓"；cats 视频无音轨 | 11 月中期演示可能缺音频能力 |
| 面部暴露占比 | 中期验收要求上报此指标 | 原标注类目无对应类（cat_face 本周新增） | 验收指标现已可对齐 |
| MoViNet | 功能 2 点名 | 训练 registry 无此族 | 端侧触发式识别可能需临时接入 |
| 硬件方工具链 | 第一阶段要求 7-8 月完成硬件装配+驱动 | 工具链预计 11 月到位 | 端侧部署链路阻塞风险 |

### 类目与判据

- **夜间帧判定修正**：家用摄像头的红外夜视帧被补光灯打亮，单纯用"画面亮度"会漏判（亮但不饱和、无色），改为"亮度低或饱和度低"才能正确识别
- **类目文档**：v0.1 已写入团队 wiki，待评审定稿

## 三、本周产出（团队资产）

- **3 个 cats-v1 模型微调 checkpoint**（tsm-resnet50 / slowonly-resnet50 / timesformer-divst，best_epoch=10/10/1）
- **Speed Run 评测结果**（tsm-resnet50 92%、slowonly 91%、timesformer 81%，101 段切片）
- **检测预标注包**：1142 帧（每 2 秒 1 帧），含可直接导入标注平台的预测框与通用 YOLO 格式
- **8 段抽样画框视频**（白天 4 / 夜视 4）供人工看片复核选型结论
- **Live 模块**：8/9 子任务完成（除 t11-6 路由导航外），GPU 推理可用，端到端实时推理 demo 通路打通
- **Speed Run 三项产品能力**：run_name 批次 / --custom 微调模型 / --ann-file GT
- **二期任务树**：t12/t13/t14/t15 全部就绪（已纳入任务系统）
- **管理视频自动转码**：H.264 + faststart，让 wiki 笔记视频浏览器可播
- **Web 前端性能优化**：dev_local.sh 起本地 vite，domContentLoaded 783ms → 238ms
- **标注类目定稿文档**（v0.1，待评审定稿）
- **骨架路线复盘**写入团队 wiki：检测+跟踪落地后骨架路线以"线上特征"角色可复活

## 四、待负责人协调的决策项

| 决策项 | 选项 | 影响 | 建议时限 |
|---|---|---|---|
| **音频缺口** | A 现有摄像头能否采音频？B RV1126B 底板 BOM 是否含 MIC？C 是否要向度小满问第三方方案？ | 硬件本周回到校方后即可实测音频链路；YAMNet 是否进 P1（11 月中期演示）仍取决于此 | **W34-W35** 拿到硬件即测；否则错过音频数据积累窗口 |
| **drinking 类（饮水动作，仅 9 段来自 2 段原始视频）** | A 补采（找采集方）；B 并入 eating，靠检测+区域规则区分水/食盆 | 本周 speedrun drinking 6/6 全误 = 实测铁证，B 决策无悬念 | 数据审计启动前（08-16 起） |
| **MoViNet 接入** | A 按任务书点名接入（端侧轻量视频模型）；B 用现有 SlowFast/TSM 替代并论证 | 端侧触发式分析能力 | 9 月跟踪启动前 |
| **多猫身份补采** | 何时启动补采？用谁家的家用摄像头？ | 多猫跟踪可行性的前提 | 跟踪启动前 |

## 五、校方后续规划（向度小满同步）

> **校方只承诺自己交付的范围**——硬件开发、装配、驱动等由硬件方负责的事项，明确标注"待硬件方"，不替对方承诺时间。

| 事项 | 具体内容 | 计划完成时间 |
|---|---|---|
| **端侧模型（校方负责）** | | |
| 数据集构建 | cats_v2 数据审计（消泄漏 + drinking 决策 + 清洗） | 08-31 |
| | 9 类标注类目评审定稿（v1.0） | 08-22 |
| | cats_v2 + 标注抽样配比（夜视+蜷缩形态重点覆盖）确认 | 09-15 |
| | 标注实施完成 | 10-15 |
| 搭建训练框架 | mmaction2 训练栈（已完成，含 28 模型注册） | ✓ |
| | YOLO11 端侧训练适配 | 09-10 |
| 端侧模型确定 | 动作识别主线（t12）：tsm-resnet50 当前为 speedrun 最优候选（92%）；9 月做两阶段微调后产出选型报告 | 09-24 |
| | 检测主线（t13）：YOLO11 微调训练 + 选型报告 | 09-30 |
| | 多目标跟踪（BoT-SORT）接入 | 10-25 |
| | 区域与时序事件引擎打通 | 11-15 |
| Live 模块 | t11-6 路由 + 导航 + wiki 链接（剩余 1/9） | W34 |
| 效果验收 | 行为识别 ≥75% / 漏检率 ≤10%（中期验收硬指标） | 11-30 |
| 模型移植 | ONNX 导出（不依赖硬件方） | 11-20 |
| | RKNN 转换 + INT8 量化（依赖硬件方工具链 11 月初到位） | 12-31 |
| **硬件层面（硬件方负责，校方仅参与联调与验收）** | | |
| 核心板装配 | RV1126B 核心板（含底板）已装配 | **W34 到** |
| 相机驱动调试 | 任务书第一阶段要求 | W34-W36（驱动调试窗口） |
| 端云系统联调 | 校方参与：上报/统计/直播/跟拍/设备控制/回放/亮点抓通（任务书要求） | 11-30 |
| 原型机与 BOM 交付 | 校方参与验收（任务书结项要求） | 02-28（结项） |

> **风险与依赖**：模型移植中的 RKNN 转换依赖硬件方工具链到位（原计划 11 月初，硬件本周提前回来后工具链可能同步提前，需与硬件方确认）；其余硬件层面依赖项已闭环。