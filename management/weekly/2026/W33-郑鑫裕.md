# 周报 — 郑鑫裕 — 2026 第 W33 周（08-10 ~ 08-16）

> 汇报对象：项目负责人；汇报重点：本周产出的**决策依据**与**待决策项**

## 一、本周一句话

为 9 月 YOLO11 检测微调完成了第一阶段选型准备——量化了通用预训练模型在 cats 数据集上的真实表现（夜视全盲、形态分化明显），定稿了 9 类标注体系，**确认微调必须做并明确了标注重点配比**。同时识别出 4 项任务书与现有计划之间的缺口，需要负责人协调。

## 二、对决策有影响的关键发现

### 体检数据：预训练模型直接测试不可用，微调必须做

体检在 GPU 服务器（4090）上用公开预训练的目标检测模型对全部 79 段 cats 视频跑了一遍（未做任何训练直接测试），关键数字：

| 场景 | 猫的检出率 | 含义 |
|---|---|---|
| **夜间红外帧**（家用摄像头夜间自动切换的成像模式） | **10-11%** | 公开训练集里没有红外样本，漏检率约 89% |
| 白天帧（按画面亮度判断） | 35% | 数字看似不低，但分母含大量"没猫"的空帧 |
| **有猫帧的召回代理**（人工标注的猫行为段内） | **48.5-48.7%** | 修正空帧稀释后的真实水平 |
| 形态：猫在盆边（进食/饮水） | **91-99%** | 标准姿态，公开模型擅长 |
| 形态：猫蜷缩/趴卧（舔毛/静止） | **10-24%** | 非典型姿态，公开模型基本失效 |

**结论**：YOLO11 检测架构可用，端侧档位（轻量版）即可满足精度要求，但必须用我们的数据微调；夜视帧 + 蜷缩形态两个盲区决定 9 月标注必须重点覆盖这两类帧。

### 为什么必须微调：9 类标注里 COCO 直接覆盖的只有 2 个

通用预训练数据集中可直接用的类目与任务书要求的类目对得上吗？逐类核对：

| 类目 | 来源 | 是否需要人工标 |
|---|---|---|
| `cat`（猫） | COCO 原生 | 不需要 |
| `person`（人） | COCO 原生 | 不需要 |
| `food_bowl` / `water_bowl`（食盆/水盆） | 项目自定义（COCO 只有笼统的 "bowl"） | **需要**——且需人工区分食/水 |
| `litter_box`（猫砂盆） | 项目自定义 | **需要** |
| `toy`（猫玩具） | 项目自定义 | **需要** |
| `door_window`（门窗） | 项目自定义 | **需要** |
| `cat_face`（猫脸） | 本周新增，为支撑中期验收"面部暴露占比"指标 | **需要** |
| `bowl_unspecified`（盆待细分） | 机器占位类（模型无法区分时占位） | 人工必须修正为 food_bowl/water_bowl |

**直接结论**：预训练模型可直接覆盖 2 个类（cat、person），其余 6+ 个类全靠 9 月人工标注——这是"微调必须做"的根本原因，也是为什么"标注配比"（夜视帧 + 蜷缩形态重点覆盖）必须现在定下来。

### 任务书与现有计划的 4 项缺口

| 缺口 | 任务书要求 | 现状 | 影响 |
|---|---|---|---|
| 音频事件识别 | 功能 1 明确列出 YAMNet（猫叫、撞击声） | phase2 计划标"暂缓"；cats 视频无音轨 | 11 月中期演示可能缺音频能力 |
| 面部暴露占比 | 中期验收要求上报此指标 | 原标注类目无对应类 | 验收指标无数据来源 |
| MoViNet | 功能 2 点名 | 训练 registry 无此族 | 端侧触发式识别可能需临时接入 |
| 硬件方工具链 | 第一阶段要求 7-8 月完成硬件装配+驱动 | 工具链预计 11 月到位 | 端侧部署链路阻塞风险 |

### 类目与判据

- **夜间帧判定修正**：家用摄像头的红外夜视帧被补光灯打亮，单纯用"画面亮度"会漏判（亮但不饱和、无色），改为"亮度低或饱和度低"才能正确识别
- **类目文档**：v0.1 已写入团队 wiki，待评审定稿

## 三、本周产出（团队资产）

- 标注类目定稿文档（v0.1，待评审定稿）
- 预标注包：1142 帧（每 2 秒 1 帧），含可直接导入标注平台的预测框与通用 YOLO 格式
- 8 段抽样画框视频（白天 4 / 夜视 4）供人工看片复核选型结论
- 骨架路线复盘写入团队 wiki：检测+跟踪落地后骨架路线以"线上特征"角色可复活，不再是"训练数据"角色

## 四、待负责人协调的决策项

| 决策项 | 选项 | 影响 | 建议时限 |
|---|---|---|---|
| **音频缺口** | A 现有摄像头能否采音频？B RV1126B 底板 BOM 是否含 MIC？C 是否要向度小满问第三方方案？ | YAMNet 是否进 P1（11 月中期演示）；建模可晚但采集日历不可压 | **本周内**确认；否则错过音频数据积累窗口 |
| **drinking 类（饮水动作，仅 9 段来自 2 段原始视频）** | A 补采（找采集方）；B 并入 eating，靠检测+区域规则区分水/食盆 | 影响 cats_v2 数据集类目；drinking 单独学习样本不够 | 数据审计启动前（08-16 起） |
| **MoViNet 接入** | A 按任务书点名接入（端侧轻量视频模型）；B 用现有 SlowFast/TSM 替代并论证 | 端侧触发式分析能力 | 9 月跟踪启动前 |
| **多猫身份补采** | 何时启动补采？用谁家的家用摄像头？ | 多猫跟踪可行性的前提 | 跟踪启动前 |

## 五、关键节点节奏（无需协调，自行推进）

| 节点 | 时间 | 状态 |
|---|---|---|
| 动作识别精度攻坚（数据修复 + 两阶段训练） | 08-16 启动 | 本周内启动，猫行为识别 75% 硬指标 |
| YOLO11 检测微调 | 09-01 启动 | 选型证据与预标注包已就绪 |
| M4 中期验收 | 11-30 | 75%/10% 硬指标 + 端云集成 |

本周已为精度攻坚与 YOLO11 检测两个节点完成准备工作，两个节点不再有阻塞决策项，待负责人协调的项均不影响关键路径。