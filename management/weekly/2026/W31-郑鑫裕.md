# 周报 — 郑鑫裕 — 2026 第 W31 周

> 时间范围：07-27（周一）~ 08-02（周日）

## 本周工作总结

### 1. pet_action_mammal_v0 接入训练与基线模型批量训练

将上周构建的 pet_action_mammal_v0 数据集正式接入 mmaction2 训练流水线，完成 TSN、VideoMAE、VideoMAEv2 等识别模型的 config 适配（含 256px 标准 config），并实现批量训练所有已注册识别模型的能力，目前已开始产出首批基线训练结果。

### 2. 训练体系完善

围绕训练全流程补齐了管理与观测能力：

- 训练 run 管理脚本（启动 / 断点续训 / 删除），训练高级超参可通过 override config 与 cfg-options 双轨配置
- 训练详情页：TensorBoard 风格的 loss / acc / lr 实时曲线，训练中定期自动生成验证集预测可视化样本
- checkpoint 拆分存储（weights / optimizer / json sidecar），支持 resume 时全量重建训练状态
- 训练结果页曲线改为多模型叠加对比，便于横向比较不同 run

### 3. 模型评估与部署指标

- speed run 增加 GPU 平均利用率与 RTF 指标，并新增模型大小 + 推理速度基准测试（训后自动测量并回填，前端列表直接展示）
- 接入 4 个 AVA 时空检测模型（SlowFast、VideoMAE、ACRN、LFB）至 speed run 流程，为后续细粒度动作检测做准备

### 4. 数据集管理与文档

- 上线数据集管理界面：浏览 / 分页 / 视频封面图 / 在线预览
- 沉淀模型接入指南与 datasets / training / testing 等 skill 文档，规范后续模型与数据集接入流程

## 实验记录

| 实验名称 | 数据集 | 指标 | 结果 | 备注 |
|----------|--------|------|------|------|
| 识别模型批量基线训练 | pet_action_mammal_v0 | Top-1 acc | 进行中 | TSN / VideoMAE / VideoMAEv2 等，首批 run 已启动 |
| AVA 检测模型 speed run | UCF101 子集 | GPU util / RTF | 已完成 | 验证 4 个检测模型推理流程可用 |

## 遇到的问题与解决方案

| 问题 | 状态 | 解决方案 / 进展 |
|------|------|-----------------|
| 训练可视化样本类别数不匹配（8 类 ckpt 加载到 2 类 model） | ✅ 已解决 | 生成时显式传 num_classes 覆盖 config |
| resume 后旧 optimizer checkpoint 残留 | ✅ 已解决 | checkpoint 拆分 + prune 改无状态扫盘 |
| 远程 GPU 被其他用户占用 | 🟢 已规避 | 训练 launcher 切换到空闲 GPU 并调整 batch size |

## 下周计划

- [ ] 完成 TSN / TSM 基线实验，记录 Top-1、Macro-F1、各类召回率与混淆矩阵
- [ ] 实施人体预训练模型的迁移基线实验与四足动物数据集 fine-tune 实验
- [ ] 重点测试参数量更小、适合摄像头端侧部署的轻量模型
- [ ] 初步验证 SuperAnimal 姿态分支对走 / 跑 / 跳等动作的增益

## 备注

本周训练流水线已基本打通（数据接入 → 批量训练 → 实时观测 → 基准评估），下周重点是产出并分析首批基线结果。
