## 成果

- 截屏功能：canvas.drawImage → toDataURL → 下载本地 + POST 上传到后端
- Web Audio API 添加快门声
- 截屏列表组件 ScreenshotGallery.vue

## 方法

- 前端 canvas 截取当前视频帧
- base64 data URL 格式上传
- 后端解析 base64 保存到 `live/screenshots/` 目录
