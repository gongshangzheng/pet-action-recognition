## 成果

- Live.vue 接入 EventSource 接收 SSE
- segments[] 数组收集推理结果
- video.currentTime 落段 → OSD 显示 label + score
- 进度条画色块标识已识别段
- 右侧段列表实时更新
- 切换/关闭时 close() 清理连接

## 方法

- EventSource API 原生支持
- 时间轴同步：currentTime → 当前段 → overlay 叠加
