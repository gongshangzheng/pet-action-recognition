## 成果

- 新建 `web/src/components/live/SourceManageModal.vue`：摄像头源管理弹窗
- 表单字段：name / alias / stream_url / storage_path / is_active
- 对接 `/api/live/sources` CRUD API

## 方法

- Naive UI n-modal + n-form 实现
- 新增/编辑共用一个弹窗，根据 source 参数区分模式
