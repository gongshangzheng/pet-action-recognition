# 日报 — 张三

## 今日完成

- 完成用户登录模块的 JWT 刷新 token 逻辑
- 修复了分页查询 offset 为负数时的 500 错误
- 代码审查：李四的论文导入 PR（#42）

## 遇到的问题

- SQLAlchemy 的 `async session` 在并发场景下偶现连接池耗尽，暂用 `pool_size=20` 缓解
- 需要和前端对齐 token 刷新的 401 响应格式

## 明日计划

- 联调前端 token 刷新流程
- 开始写单元测试覆盖 auth 模块
