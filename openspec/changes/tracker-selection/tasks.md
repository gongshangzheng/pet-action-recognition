# Tasks: tracker-selection

> 总管：`pet-motion-latent-pipeline` §4A。**重启（2026-09-17）**：触发条件由「等出现多猫」改为「主动核实 + 用户批准」。
> 启动前请核对 BoxMOT Re-ID 权重是否就位（BotSort/DeepOcSort 契约测试当前跳过）。

## 0. 前置闸门：多猫核实（**必须先行**）

- [ ] 0.1 **多猫核实**：抽 6–10 段原始视频（含两个告警段 100505 / 180330 + 随机段），人工看每段画面里有几只猫；**逐户清点**（`cui` / `jiang` 两个家庭各几只）
- [ ] 0.2 核实**当前管线在多猫段的行为**：取一段含多猫的视频跑 `pet_detect_track.py`，检查 `cat_box` 序列是否在两只猫之间跳变（→ 确认/否证「argmax 静默出错」）
- [ ] 0.3 结论回写：若有多猫 → ① 受影响段清单（需重跑）② 本 change 的实施优先级上调；若无 → 记录「单猫假设已验证」，并把触发条件回调为「多猫数据出现」

## 1. 选型实验

- [ ] 1.1 GT 制作：抽 3–5 段白天视频（**含已核实的多猫段**），人工核对 track_id
- [ ] 1.2 固定检测源缓存 + 运行候选（ByteTrack / OC-SORT / BoT-SORT / DeepSORT）
- [ ] 1.3 **两组形态对比**：A. 关联器只跑抽样帧（配插值填中间帧）× B. 关联器跑全帧率（对照复核 D1 结论）
- [ ] 1.4 **是否用卡尔曼预测参与框平滑**：只贴 id（保观感）vs 参与平滑（可能引入抖动，需实测复核 D1 的「跟踪精度 ≠ 观感质量」结论）
- [ ] 1.5 指标计算：IDF1（主）/ IDSW / 轨迹碎片数 / 框平滑度 → 选型报告
- [ ] 1.6 结论 → 决定 `spot-check-cli` 默认跟踪器 → archive

> 备查记录：检测器侧误报抑制阈值扫描已预跑——0.3→0.5 均无法抑制沙发超宽框（高置信检出）；沙发漂移由渲染层尺寸离群过滤兜底，multi-object-detect-gate 验收后升级为沙发框语义校验。
