## 1. 选型与准备

- [ ] 1.1 获取候选权重（SAM2 / RVM / BiRefNet）并验证可加载（pet，GPU 前 `nvidia-smi` 查占用）
- [ ] 1.2 小规模选型实测：3-5 段样本（followcam / mammal_v0 / cats_v1 各取样）跑三方对比，按掩码质量、时序稳定性、速度裁决 → 定选型并回写 design D2
- [ ] 1.3 定掩码后处理参数（膨胀/腐蚀像素、面积阈值、IoU 漂移阈值），记录到 design D3

## 2. 抠像管线实现

- [ ] 2.1 抠像 CLI：输入视频 + 猫检测框 → 掩码传播 → 二值掩码 → 背景置黑合成 → 输出 mp4（帧数/帧率/分辨率逐帧对齐，不覆盖原视频）
- [ ] 2.2 检测框接入：复用现有 GroundingDINO 输出；多猫场景掩码取并集
- [ ] 2.3 质量闸门：面积突变 / 时序漂移 / 检测丢失帧的检出，回退最近有效掩码并标记告警（不静默输出脏数据）
- [ ] 2.4 单段端到端验证：followcam / mammal_v0 / cats_v1 各 1 段，人工目检抠像质量 + 核对帧对齐

## 3. 批处理与验收

- [ ] 3.1 批处理脚本：遍历语料根目录 + 产出清单（每段成功/失败/告警 + 失败原因）+ 支持续跑（跳过已完成）
- [ ] 3.2 全量语料批处理（followcam 34 + mammal_v0 2234 + cats_v1 717；pet 4090）
- [ ] 3.3 清单核对 + 抽样目检（≥10 段，含全部告警段）+ 失败段归因
- [ ] 3.4 交付说明 + 下游接口约定（抠像语料路径供 `identity-action-tokenizer` 阶段 B 消费）
